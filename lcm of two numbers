#include<stdio.h>
int main()
{
	int a,b,l;
	
	printf("enter two numbers to find LCM:");
	scanf("%d",&a);
	scanf("%d",&b);

    for(l=1;l<=(a*b);l++)
   {
	if(l%a==0 && l%b==0)
	  break;
   }
  printf("\nLCM of %d and %d is: %d",a,b,l);
  return 0;
}
